eureka:
  client:
    service-url:
      default-zone: http://eureka-server
      
  cloud:
    gateway:
      routes:
      - id: fullstackserver
        uri: lb://http://localhost:8081
        predicates:
        - Path=/**      
      
