eureka:
  client:
    service-url:
      default-zone: http://eureka-server
      
  cloud:
    gateway:
      routes:
      - id: fullstackserver
        uri: lb://FULLSTACK-SERVICE
        predicates:
        - Path=/**      
      
